<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.github.perniteo.sudoku.repository.SudokuMapper">

  <insert id="insertSudoku">
    INSERT INTO board
      (level, start_board, answer_board, created_at)
    VALUES (
      #{difficulty},
      #{startBoard}::jsonb,
      #{answerBoard}::jsonb,
      NOW()
    )
  </insert>

  <select id="findRandomByDifficulty" resultType="io.github.perniteo.sudoku.dto.SudokuBoardRow">
    SELECT
      id            AS boardId,
      level         AS difficulty,
      start_board   AS startJson,
      answer_board  AS answerJson
    FROM board
    WHERE level = #{difficulty}
    ORDER BY RANDOM()
    LIMIT 1
  </select>

</mapper>